크롤# 데이터베이스 설정
DATABASE_URL=postgresql://housing_user:housing_password@localhost:5432/seoul_housing

# Redis 설정
REDIS_URL=redis://localhost:6379

# 보안 설정
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 환경 설정
ENVIRONMENT=development
DEBUG=true

# 로깅 설정
LOG_LEVEL=INFO
LOG_FORMAT=json

# 크롤링 설정
CRAWLING_INTERVAL_HOURS=6
MAX_RETRIES=3
REQUEST_TIMEOUT=30

# 파일 저장 설정
DATA_DIR=./data
LOGS_DIR=./logs
MAX_FILE_SIZE=104857600

# API 설정
API_V1_STR=/api/v1
PROJECT_NAME=서울주택도시공사 주택지원 봇

# 페이지네이션 설정
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# CORS 설정 (쉼표로 구분)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001
