[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Real_Estate-for-the-Young"
version = "0.1.0"
description = "Real_Estate-for-the-Young"
readme = "README.md"
requires-python = ">=3.12,<3.14"

[tool.setuptools.packages.find]
where = ["."]
include = ["backend*"]    # discover backend, backend.services, ...

[project.scripts]
data-collection = "backend.services.data_collection.cli.__main__:main"
data-db = "backend.db.db_manager:main"
data-load = "backend.services.data_collection.cli.load_normalized_data:main"
