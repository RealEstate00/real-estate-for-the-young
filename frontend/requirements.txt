# Streamlit 웹 애플리케이션
streamlit>=1.28.0

# 환경 변수 관리
python-dotenv>=1.0.0

# Hugging Face Transformers (대화 요약용)
transformers>=4.35.0
torch>=2.0.0

# 기존 백엔드 LangChain 의존성
langchain>=0.1.0
langchain-community>=0.0.10
langchain-groq>=0.1.0
langchain-openai>=0.1.0

# 데이터 처리
pandas>=2.0.0

# 이미지 처리 (찜목록 카드용)
pillow>=10.0.0

# JSON 처리 (기본 라이브러리이므로 별도 설치 불필요)
# datetime (기본 라이브러리)
# pathlib (기본 라이브러리)
